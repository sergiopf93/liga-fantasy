name: Actualización 7:00
on:
  schedule:
    - cron: '0 5 * * *'
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install requests pyyaml
      - name: Reclamar recompensa diaria
        env:
          LALIGA_TOKEN: ${{ secrets.LALIGA_TOKEN }}
          TEAM_ID: ${{ secrets.TEAM_ID }}
          LEAGUE_ID: ${{ secrets.LEAGUE_ID }}
        run: python scripts/daily_reward.py || echo "Recompensa no disponible o ya reclamada"

      - name: Actualización matutina
        env:
          LALIGA_TOKEN: ${{ secrets.LALIGA_TOKEN }}
          TEAM_ID: ${{ secrets.TEAM_ID }}
          LEAGUE_ID: ${{ secrets.LEAGUE_ID }}
        run: python scripts/generate_report.py
      - name: Guardar histórico diario
        env:
          LALIGA_TOKEN: ${{ secrets.LALIGA_TOKEN }}
          TEAM_ID: ${{ secrets.TEAM_ID }}
          LEAGUE_ID: ${{ secrets.LEAGUE_ID }}
        run: python scripts/save_history.py || echo "Error guardando histórico"

      - name: Commit
        run: |
          git config user.name "Fantasy Bot"
          git config user.email "bot@liga-fantasy"
          git add data/
          git diff --staged --quiet || git commit -m "Actualización 7:00 - $(date '+%d/%m %H:%M')"
          git push
      - name: Preparar site
        run: |
          mkdir -p _site/data
          cp frontend/index.html _site/index.html
          mkdir -p _site/data/reports
          cp data/reports/*.json _site/data/reports/ 2>/dev/null || true
          cp data/reports/*.html _site/data/reports/ 2>/dev/null || true
          for f in data/team.json data/market.json data/report.json data/rivals.json data/decisions.json data/reward.json data/analyst_latest.json; do
            [ -f "$f" ] && cp "$f" _site/data/ || echo "No existe: $f"
          done
      - name: Deploy
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./_site
